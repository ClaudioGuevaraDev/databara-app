{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splash",
  "description": "Permissions for the splash window: receive startup progress events from the main window.",
  "windows": ["splash"],
  "permissions": ["core:event:default"]
}
